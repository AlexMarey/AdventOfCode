const {input} =require('./input');
const {calcHorizontalDepth, calcHorizontalAim} = require('./dive');

console.log(`Part One: ${calcHorizontalDepth(input)}`);
console.log(`Part One: ${calcHorizontalAim(input)}`);